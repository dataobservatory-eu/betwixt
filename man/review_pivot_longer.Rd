% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review_pivot_longer.R
\name{review_pivot_longer}
\alias{review_pivot_longer}
\title{Project a Betwixt dataset to canonical long form}
\usage{
review_pivot_longer(x)
}
\arguments{
\item{x}{A Betwixt candidate or reviewed data frame.}
}
\value{
A tibble with columns \code{row_id}, \code{assertion_id}, \code{role},
\code{subject}, \code{predicate}, and \code{value}.
}
\description{
Projects a Betwixt candidate or reviewed dataset into the canonical
assertion representation.

The projection preserves the order of rows, fields, and pipe-separated
values in the supplied dataset. \code{row_id} identifies the originating
wide-form tuple. \code{assertion_id} identifies the position of the assertion
in the canonical long projection.

Dataset identity is represented with \code{row_id = 0}. Domain assertions,
input-to-subject relations, contextual statements, and row comments are
assigned distinct roles.

Range and definition columns describe candidate fields and are not
themselves projected as assertions. Review-state columns such as
qualifications, finalisation, and outcomes are likewise not domain
assertions.
}
