% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/betwixt_render.R
\name{betwixt_render}
\alias{betwixt_render}
\title{Render a Betwixt review interface}
\usage{
betwixt_render(
  claim,
  range = NULL,
  evidence_media_file = NULL,
  evidence_url = NULL,
  descriptive = NULL,
  assertions = NULL,
  context = NULL,
  con = NULL,
  title = "Betwixt Review",
  description = "Please review the following claims.",
  betwixt_id = "P1234",
  template = "long_review"
)
}
\arguments{
\item{claim}{A data frame containing the material to render.}

\item{range}{Optional data frame defining proposed values for reviewable
assertions.}

\item{evidence_media_file}{Optional character string naming the column
containing the media resource presented as evidence.}

\item{evidence_url}{Optional character string naming the column containing
a URL to supporting evidence.}

\item{descriptive}{Optional character vector naming columns presented as
descriptive metadata.}

\item{assertions}{Optional character vector naming columns presented as
reviewable assertions.}

\item{context}{Optional character vector naming columns presented as
contextual information.}

\item{con}{Optional output connection or file path. If \code{NULL}, return the
rendered HTML as a character string.}

\item{title}{Character string giving the review title.}

\item{description}{Character string describing the review task.}

\item{betwixt_id}{Character string identifying the review artefact.}

\item{template}{Character string identifying the review template. Must be
\code{"long_review"} or \code{"wide_review"}.}
}
\value{
If \code{con} is \code{NULL}, a character string containing the rendered
HTML. Otherwise, \code{con} is returned invisibly.
}
\description{
Renders a human-readable review interface from a rectangular dataset.
}
\details{
A review interface may contain four blocks:
\itemize{
\item \strong{evidence}, containing media or a source URL inspected by the reviewer;
\item \strong{descriptive metadata}, containing information that may be displayed
or edited without constituting an epistemic review decision;
\item \strong{semantic assertions}, containing the information under review; and
\item \strong{context}, containing additional information that supports the review.
}

The columns belonging to these blocks are specified explicitly. Their
roles are not inferred from column names.

Long and wide reviews are alternative human-readable projections of
semantic assertions. They do not define different semantic models.

In a long review, \code{assertions} identifies the assertion components exposed
as columns. For example, \code{c("subject", "predicate", "value")} exposes a
complete elementary assertion, while \code{c("predicate", "value")} may be used
when the subject is implicit in the evidence under review.

A long review may also expose multiple proposed values, for example
\code{c("predicate", "value1", "value2")}.

In a wide review, each column named by \code{assertions} represents a predicate
and its cells contain the corresponding reviewable values.

Sequential row numbers are generated by the renderer. They are part of the
review interface and do not need to be present in \code{claim}.
}
