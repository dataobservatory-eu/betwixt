% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_review_wide.R
\name{project_review_wide}
\alias{project_review_wide}
\title{Project a Betwixt review to aligned wide planes}
\usage{
project_review_wide(review)
}
\arguments{
\item{review}{A Betwixt review object returned by \code{\link[=read_review]{read_review()}}.}
}
\value{
A data frame containing three structurally aligned wide planes,
identified by the \code{plane} column as \code{candidate}, \code{reviewed}, or \code{status}.
Review provenance is retained in the \code{provenance} attribute.
}
\description{
Projects a Betwixt review into candidate, reviewed, and status planes while
preserving the wide structure of the original review.
}
\details{
The candidate plane contains the assertions presented for review. The
reviewed plane contains the values persisted by the reviewer. The status
plane describes the review state of each reviewable field.

Semantic assertions may carry explicit \code{defer} or \code{reject} qualifications.
These take precedence over states inferred from candidate and reviewed
values. Descriptive fields currently use inferred review states only.

For a finalised row, an unchanged value is \code{corroborated}, a changed
non-empty value is \code{corrected}, deletion of a candidate value is \code{rejected},
and an absent descriptive assertion that remains absent is \code{deferred}.
Values in rows that have not been finalised are \code{pending}, unless an
explicit semantic qualification applies.

Reviewable fields include descriptive fields such as \code{label}, \code{description},
\code{alternative_label}, and \code{alternative_description}, when present, together
with semantic assertion fields represented by \verb{_qualification} columns.

An empty reviewed descriptive value is interpreted as rejection only when
the corresponding candidate value was non-empty and the row was finalised.
If both candidate and reviewed descriptive values are missing in a finalised
row, the assertion is interpreted as deferred rather than rejected.
}
